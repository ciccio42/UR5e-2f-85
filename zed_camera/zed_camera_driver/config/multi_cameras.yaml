cameras:
  - camera_name: zed_front
    serial_number: 16450494

  - camera_name: zed_left
    serial_number: 11990492

  - camera_name: zed_right
    serial_number: 15689351

  - camera_name: zed_gripper
    serial_number: 16702584
    # camera moves with the arm, so it has no fixed relationship to gravity:
    # gravity-aligned pose tracking (which assumes a static camera) doesn't
    # apply here. Overrides the global gravity_alignment launch argument for
    # this camera only.
    gravity_alignment: false